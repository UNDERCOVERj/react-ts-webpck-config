import * as React from 'react';
import '../scss/App.scss';

interface StaticProps {};

export default class App extends React.Component<StaticProps, {}> {
    render() {
        return <div className = 'login-body'>dfjdioeiojio</div>
    }
}